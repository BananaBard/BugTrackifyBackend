import supabase from "../../infra/supabaseClient";
import getErrorMessage from "../../utils/getErrorMessage";
import {Tables} from '../../supabase';


const createIncidentService = async ({  }: Tables<'Incidents'>) => {
    
}

export {createIncidentService};